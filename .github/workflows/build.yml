on: push

jobs:
    build-apk:
      name: buildSignedAPK
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - run : |
            echo $"{{ secrets.RELEASE_KEY_ALIAS }}"